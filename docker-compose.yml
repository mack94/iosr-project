version: '2.0'
services:
  loadbalancer:
    build: iosr-loadbalancer
    ports:
      - "8765:8765"
  eureka:
    build: iosr-server
  project:
    build: iosr-project
  manipulator:
    build: iosr-manipulation-service

